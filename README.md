# Backend API for TafshanApp.
## Make sure to run "npm install" on first try to download any dependencies.
## .gitignore will list folders and files that git will not track and push to the master branch.
